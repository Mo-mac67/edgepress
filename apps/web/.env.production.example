# Copy to `.env.production`. Loaded by `next build`. Anything that bakes absolute
# URLs at build time (sitemap.xml, canonical, OG tags, JSON-LD) reads SITE_URL
# here — set it to YOUR public URL before building for production.
SITE_URL=https://your-site.example.com
