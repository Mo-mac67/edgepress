# Loaded by `next build`. The public pages are force-static, so anything that
# bakes absolute URLs (sitemap.xml, metadataBase/OG tags, JSON-LD) reads
# SITE_URL at BUILD time — the wrangler.jsonc var only covers runtime code.
SITE_URL=https://edgepress.mh-bamzadeh.workers.dev
